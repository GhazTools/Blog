import MarkdownProcessor from "./markdown_processor";

function AboutPage() {
    return (
        <MarkdownProcessor markdown="# This about page is in construction" />
    );
}

export default AboutPage;
